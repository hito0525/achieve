<% if current_user.likes.find_by(blog_id: blog.id) %>
    <%= link_to 'いいね！を取り消す', unlike_path(blog.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to 'いいね！', like_path(blog.id), method: :post, remote: true %>
<% end %>

